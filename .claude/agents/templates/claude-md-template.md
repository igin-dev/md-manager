# [Название] — [одна строка что это]

<!-- 
ИНСТРУКЦИЯ ПО ЗАПОЛНЕНИЮ (удали после заполнения):
Включай секцию ТОЛЬКО если её содержимое неочевидно из кода.
Тест: агент прочитает 5 файлов проекта — поймёт ли без этой секции?
Если да — не включай.
-->

## Навигация
<!-- Только для монорепо. Карта субпроектов — агент не знает что нужно читать дочерний CLAUDE.md -->

| Директория | Описание | CLAUDE.md |
|---|---|---|
| `[path]/` | [что это] | `[path]/CLAUDE.md` |

## Автозапуск
<!-- Только если компоненты запускаются автоматически — Task Scheduler, cron, systemd -->
<!-- Критично: агент может запустить процесс который уже запущен -->

`[Задача]` запускает `[скрипт]` автоматически при [событии].

## MCP-серверы
<!-- Только если настроены в .mcp.json — агент не знает о них без явного указания -->

- `[server-name]` — [для чего использовать]

## Запуск
<!-- Только если нестандартный ИЛИ важно знать что уже запущено -->
<!-- НЕ включай очевидные команды типа `python app.py` или `npm start` -->

| Сценарий | Команда |
|---|---|
| [нестандартный сценарий] | `[команда]` |

## Gotchas
<!-- Неочевидное поведение которое сломает работу. С примером и причиной. -->
<!-- НЕ включай стандартные паттерны которые агент знает сам -->

**[Название gotcha]** — [описание неочевидного поведения].
Причина: [почему так].
```python
# Правильно
[пример]
# Неправильно
[пример]
```

## Запреты
<!-- Только с объяснением причины. Без причины агент проигнорирует. -->
<!-- НЕ включай очевидные запреты типа "не хардкодь пароли" -->

**[Что запрещено]** — [причина]. Используй [альтернативу].

## Ссылки

@ARCHITECTURE.md — [когда читать]
@CONVENTIONS.md — [когда читать]
